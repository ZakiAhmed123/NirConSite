<div class="product-index-container">

  <%= render '/layouts/nav_bar.html.erb'%>

<div class="index-title-block">
  <div class="i-image">
    <%= image_tag 'Easy_Embeds.png', class:'index-title-image' %>
  </div>
  <div class="i-container">


    <div class="index-desc">
      Easy Embeds has become the new standard by redefining how embeds are fabricated and sold. Long gone are the pains of today including seeking approval for shop drawings and long lead times. We keep an active inventory of embeds to allow for rapid processing and shipping. All purchases come with shop drawings and load calculations signed and sealed by a P.E. in your state. Custom embeds are also available for purchase by uploading your plans
      <%= link_to "here", new_message_path %>.
    </div>

  </div>

</div>

    <% flash.each do |name,msg| %>
    <%= content_tag :div, msg, :id => "flash", class: 'flash-message' %>
    <%end%>
    <div class="product-container">
      <% @products.limit(20).each do |product|%>
      <div class="embed-index-item">
        <%= link_to "/products/embeds/#{product.id}" do %>
        <div class="image-container">
          <%= image_tag "#{product.img_file}.jpg", {:class => "embed_picture"}%>
        </div>
        <p class="product-index-price"><%= number_to_currency(product.price)%></p>
        <p class="product-name"><%= product.name %> </p>
        <p class="product-sub">
      <%= product.info_1 %></p>
        <%end%>
      </div>

      <%end%>
    </div>

    <div class="feature-products">
      EMBED STABILIZER KITS
    </div>
    <div class="product-container">
      <% @products[20..22].each do |product|%>
      <div class="embed-index-item">
        <%= link_to "/products/embeds/#{product.id}" do %>
        <div class="image-container">
          <%= image_tag "#{product.img_file}.jpg", {:class => "embed_picture"}%>
        </div>
        <p class="product-index-price"><%= number_to_currency(product.price)%></p>
        <p class="product-name"><%= product.name %> </p>
        <p class="product-sub">
      <%= product.info_1 %></p>
        <%end%>
      </div>

      <%end%>
    </div>

  </div>
</div>

<div class="panels">
<div class="area">
  <div class="p-icon">
    <i class="fa fa-trophy fa-3x" aria-hidden="true"></i>
  </div>
<div class="p-name">
QUALITY GUARANTEED
</div>


</div>
<div class="area">
  <div class="p-icon">
    <i class="fa fa-bus fa-3x" aria-hidden="true"></i>
  </div>
<div class="p-name">
ON SITE DELIVERY
</div>


</div>
<div class="area">
  <div class="p-icon">
    <i class="fa fa-lock fa-3x" aria-hidden="true"></i>
  </div>
<div class="p-name">
SECURE PAYMENT
</div>


</div>
</div>

<%= render '/layouts/footer.html.erb'%>
